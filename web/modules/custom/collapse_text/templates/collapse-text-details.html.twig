{#
/**
 * @file
 * Default theme implementation for displaying collapse text details.
 *
 * Theme a section of collapsible text. By default, this template calls the
 * default 'theme_details' implementation, but this template can be overridden
 * to implement a custom theme just for collapsed text.
 *
 * Available variables:
 * - element: Form element containing the details of the collapsible section.
 *
 * @ingroup themeable
 */
#}
{{ element }}
